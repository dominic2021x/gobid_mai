"use client";

/**
 * Router-aligned listing query helpers for /ro (load more + API parity).
 * Filter UI state remains in `RoAuctionsViewClient` until further extraction.
 */
export { buildListingsApiParams, LISTINGS_ALLOWED_KEYS } from "@/lib/ro/roListingsApiParams";
