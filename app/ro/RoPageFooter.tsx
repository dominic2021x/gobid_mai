"use client";

import DashboardFooter from "@/components/DashboardFooter";

export default function RoPageFooter({ isDarkMode }: { isDarkMode?: boolean }) {
  return <DashboardFooter isDarkMode={isDarkMode} />;
}
